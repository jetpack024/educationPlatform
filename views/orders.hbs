
<h1>Добрый день, {{name}}</h1>
<p>Имя: {{name}}</p>
<p>Mail: {{email}}</p>
<p>Номер телефона: {{phoneNumber}}</p>
<div id="addingForm">
<h5>Для полного доступа заполните форму:</h5>
  <form  id="buttonUserForm" >

  <button id="{{id}}"  value="" class="btn btn-primary">Заполнить форму</button>
  </form>
</div>

<div id="userForm">

</div>


{{!-- 
{{#if orders.length}}
	{{#each orders}}
		<div class="row">
			<div class="col s6 offset-s3">
				<div class="card">
					<div class="card-content">
						<span class="card-title">
							Курс <small>{{_id}}</small>
						</span>
						<p class="date">{{date}}</p>
						<p><em>{{user.userId.name}}</em>({{user.userId.email}})</p>
						<ol>
							{{#each courses}}
								<li>
									{{course.title}} (x<strong>{{count}}</strong>)
								</li>
								{{/each}}
						</ol>
						<hr>
						<p>Цена: <span class="price">{{price}}</span></p>
					</div>
				</div>
			</div>
		</div>
	{{/each}}
{{else}}
	<p>Заказов пока нет</p>
{{/if}} --}}
